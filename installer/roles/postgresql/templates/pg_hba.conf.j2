# Managed by zabbix-platform M2. Local Phase-1 database access only.
local   all       postgres                                peer
local   all       all                                     peer
host    {{ zabbix_db_name }}  {{ zabbix_db_user }}  127.0.0.1/32  scram-sha-256
host    {{ zabbix_db_name }}  {{ zabbix_db_user }}  ::1/128       scram-sha-256
