# Huawei templates

No template export is stored here. Zabbix 7.0.30 stock seed data supplies the
official `Huawei VRP by SNMP` template, which is verified after installation.
This avoids an unnecessary fork of an official template.
